version: "3.5"
services:
  server:
    volumes:
      - ../tw-server:/app
      - /app/node_modules
  data:
    volumes:
      - ../tw-data:/app
      - ../tw-server/src/generated:/app/generated
      - /app/node_modules
